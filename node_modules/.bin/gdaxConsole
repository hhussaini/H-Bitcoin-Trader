#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gdax-trading-toolkit/build/src/consoles/gdaxConsole.js" "$@"
  ret=$?
else 
  node  "$basedir/../gdax-trading-toolkit/build/src/consoles/gdaxConsole.js" "$@"
  ret=$?
fi
exit $ret
